.hero-unit.width220.container
  %h3.center
    Tell us your interests!
  - current_user.user_to_interests.each do |e|
    .ability
      %span.badge.badge-warning.name
        = e.name
  = simple_form_for @interest do |f|
    = f.hidden_field :user_id, :value => current_user.id
    = f.input :interest_id, :as => :select, :multiple => true, :collection => Interest.all, :input_html => { :class => "chzn-select" }


    %button.btn.btn-primary{:type => "submit", :data => {'update-target' => 'update-container'}}
      Submit
    = link_to "Back to Home", root_path, :class => "btn btn-warning"